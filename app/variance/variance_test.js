'use strict';

describe('income.variance module', function() {

  beforeEach(module('income.variance'));

  describe('variance controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('VarianceCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});