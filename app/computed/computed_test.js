'use strict';

describe('income.computed module', function() {

  beforeEach(module('income.computed'));

  describe('computed controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('ComputedCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});